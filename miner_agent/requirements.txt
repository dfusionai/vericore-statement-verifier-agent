# Dependencies for miner_agent
# HTTP requests for API calls
requests>=2.32.5

# Bittensor and blockchain interaction (for neurons module)
bittensor>=9.9.0

# Async HTTP client (for neurons module)
aiohttp>=3.10.11

# Async file operations (for neurons module)
aiofiles>=24.1.0

# CLI framework (for neurons module)
click>=8.0.0

# Environment variable management (for neurons module)
# Note: The package name is python-dotenv, but it's imported as 'dotenv'
python-dotenv>=1.0.0

